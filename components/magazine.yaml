allOf:
  - $ref: ./content.yaml
  - required:
      - publisher
      - publish
    properties:
      publisher:
        type: string
        description: 出版社
        example: 集英社
      publish:
        type: string
        enum:
          - daily
          - weekly
          - monthly
        description: "刊行形態"
        example: weekly
